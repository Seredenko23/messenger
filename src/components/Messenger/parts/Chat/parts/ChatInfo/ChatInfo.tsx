import React, {Component} from 'react';
import './ChatInfo.scss'

class ChatInfo extends Component {
  render() {
    return (
      <div className={'chat-info'}>
        <span className={'user'}>Test Testosteron</span>
      </div>
    );
  }
}

export default ChatInfo;
